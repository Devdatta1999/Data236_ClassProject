apiVersion: apps/v1
kind: Deployment
metadata:
  name: aerive-user-service
  namespace: aerive
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aerive-user-service
  template:
    metadata:
      labels:
        app: aerive-user-service
    spec:
      containers:
      - name: user-service
        image: aerive/user-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3001
        envFrom:
        - configMapRef:
            name: aerive-config
        - secretRef:
            name: aerive-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: aerive-user-service
  namespace: aerive
spec:
  selector:
    app: aerive-user-service
  ports:
  - port: 3001
    targetPort: 3001
  type: ClusterIP

