apiVersion: apps/v1
kind: Deployment
metadata:
  name: aerive-listing-service
  namespace: aerive
spec:
  replicas: 3
  selector:
    matchLabels:
      app: aerive-listing-service
  template:
    metadata:
      labels:
        app: aerive-listing-service
    spec:
      containers:
      - name: listing-service
        image: aerive/listing-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3002
        envFrom:
        - configMapRef:
            name: aerive-config
        - secretRef:
            name: aerive-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: aerive-listing-service
  namespace: aerive
spec:
  selector:
    app: aerive-listing-service
  ports:
  - port: 3002
    targetPort: 3002
  type: ClusterIP

